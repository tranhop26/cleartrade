// DecisionHistory.jsx — Bảng lịch sử quyết định + rootHash 0G Storage link
// TODO: implement in logic phase

export default function DecisionHistory({ history }) {
  return <div>History placeholder — {history?.length ?? 0} records</div>;
}
